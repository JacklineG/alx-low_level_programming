#include "main.h"

/**
 * _abs - computes the absolute value of a number
 * @n: the number ti check
 * Return: the absolute value integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
